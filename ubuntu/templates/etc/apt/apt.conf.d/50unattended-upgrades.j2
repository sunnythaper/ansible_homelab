Unattended-Upgrade::Allowed-Origins {
        "${distro_id}:${distro_codename}-security";
        "${distro_id}:${distro_codename}-updates";
        "${distro_id}:${distro_codename}";
        "${distro_id}ESM:${distro_codename}-infra-security";
        "${distro_id}ESM:${distro_codename}";
        "${distro_id}ESMApps:${distro_codename}-apps-security";
//      "${distro_id}:${distro_codename}-proposed";
//      "${distro_id}:${distro_codename}-backports";
};

Unattended-Upgrade::Package-Blacklist {
{% for package in unattended_upgrade_Package-Blacklist %}
      "{{package}}";
{% endfor %}
};

{% if unattended_upgrade_Mail %}
Unattended-Upgrade::Mail "{{ unattended_upgrade_Mail }}";
{% if unattended_upgrade_MailOnlyOnError %}
Unattended-Upgrade::MailOnlyOnError "true";
Unattended-Upgrade::MailReport  "only-on-error";
{% endif %}
{% endif %}

Unattended-Upgrade::AutoFixInterruptedDpkg "{{ unattended_upgrade_AutoFixInterruptedDpkg }}"
Unattended-Upgrade::Automatic-Reboot "{{ unattended_upgrade_Automatic-Reboot }}";
Unattended-Upgrade::Automatic-Reboot-Time "{{ unattended_upgrade_Automatic-Reboot-Time }}";
Unattended-Upgrade::Automatic-Reboot-WithUsers "{{ unattended_upgrade_Automatic-Reboot-WithUsers }}";
Unattended-Upgrade::Debug "{{ unattended_upgrade_Debug }}"
Unattended-Upgrade::InstallOnShutdown "{{ unattended_upgrade_InstallOnShutdown }}"
Unattended-Upgrade::MinimalSteps "{{ unattended_upgrade_MinimalSteps }}"
Unattended-Upgrade::OnlyOnACPower "{{ unattended_upgrade_OnlyOnACPower }}"
Unattended-Upgrade::Remove-New-Unused-Dependencies "{{ unattended_upgrade_Remove-New-Unused-Dependencies }}"
Unattended-Upgrade::Remove-Unused-Dependencies "{{ unattended_upgrade_Remove-Unused-Dependencies }}"
Unattended-Upgrade::Remove-Unused-Kernel-Packages "{{ unattended_upgrade_Remove-Unused-Kernel-Packages }}"
Unattended-Upgrade::Skip-Updates-On-Metered-Connections "{{ unattended_upgrade_Skip-Updates-On-Metered-Connections }}"
Unattended-Upgrade::SyslogEnable "{{ unattended_upgrade_SyslogEnable }}"
Unattended-Upgrade::SyslogFacility "{{ unattended_upgrade_SyslogFacility }}"
Unattended-Upgrade::Verbose "{{ unattended_upgrade_Verbose }}"
