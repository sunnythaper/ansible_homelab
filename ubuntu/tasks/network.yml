---
- name: Copy resolv.conf to set nameserver settings
  template:
    src: etc/resolv.conf.j2
    dest: /etc/resolv.conf
    mode: 0644
    owner: root
    group: root
  notify:
    - Restart systemd